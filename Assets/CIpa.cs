using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CIpa : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Console.WriteLine("HAHHAHAH! :)");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
